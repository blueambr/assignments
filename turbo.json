{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "solution": {
      "dependsOn": ["^solution"]
    },
    "test": {
      "dependsOn": ["lint", "solution"]
    },
    "lint": {}
  }
}
