{
  "name": "assignments",
  "workspaces": [
    "solutions/*",
    "packages/*"
  ],
  "scripts": {
    "solution": "turbo run solution",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.28.4",
    "eslint": "^8.33.0",
    "eslint-config-custom": "*",
    "husky": "^8.0.0",
    "prettier": "latest",
    "turbo": "latest",
    "vitest": "^0.28.4"
  }
}
